<div class="products-page">
    <div class="row m-1">
        <%= render 'layouts/sidebar'%>
        <div class="main-content col-md-9">
            <div class="container">
                <div class="d-flex flex-wrap justify-content-center">
                    <span class=" align-self-center mr-3 d-md-none" onclick="showSidebar()">
                        <%= fa_icon "filter", class: 'btn btn-outline-primary' %>
                    </span>
                    <% if current_user.seller_role? %>
                        <div class="mr-3 align-self-center">
                            <%= link_to "Add Product", new_product_path, class: 'd-inline btn btn-outline-primary' %>
                        </div>
                    <% end %>
                    <%# Search %>
                    <%= form_tag products_path, :method => "get", class: 'search-form flex-nowrap form-inline justify-content-around' do %>
                        <%= text_field_tag :search, params[:search], class: 'form-control mx-1', placeholder: 'Search' %>
                        <%= submit_tag "Search", class: 'btn btn-dark', onclick: 'checkDimensions()' %>
                    <% end %>
                </div>
                <hr>
                <div class="products">
                    <% if @products.present? %>
                        <%= render @products %>
                    <% else %>
                        <p style="text-align: center">No Products Found</p>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</div>