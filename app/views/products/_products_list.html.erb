<%# Products %>
<div class="row products-container">
    <% if @products.present? %>
        <% @products.each do |product| %>
            <%= link_to product_path(product.id) do %>
                <div class="card col-md-4">
                    <%= (product.image.attached?) ? image_tag(product.image, size: "250x250", class: 'card-img-top') : "No Image" %>

                    <div class="card-body">
                        <h5 class="card-title"><%= product.title %></h5>
                        <p class="card-text"><%= product.price %> EGP</p>
                        <%= link_to "Add Cart", add_cart_path(product.id),method: :post, class: 'btn btn-dark', data: { confirm: 'Do you want to add ( ' + product.title + ' ) to your cart?'} %>
                    </div>
                </div>
            <% end %>
        <% end %>
    <% else %>
        <p style="text-align: center">No Products Found</p>
    <% end %>
</div>